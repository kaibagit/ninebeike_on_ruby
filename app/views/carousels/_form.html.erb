<%= form_for @carousel do |f| %>
  <% if @carousel.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@carousel.errors.count, "error") %> prohibited this carousel from being saved:</h2>

      <ul>
      <% @carousel.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
      <% if @carousel.image.present? %>
          <a href="<%= @carousel.image %>" target="_blank"><image src="<%= @carousel.image %>" style="max-width: 100px;"/></a>
      <% end %>
      <%= f.file_field :image_file %>
  </div>
  <div class="field">
    <%= f.label :ulr %><br>
    <%= f.text_field :ulr %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
